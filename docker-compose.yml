name: tune-share-hub-frontend

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tune-share-hub-frontend-app
    ports:
      - "${APP_PORT:-3000}:3000"
    restart: unless-stopped
